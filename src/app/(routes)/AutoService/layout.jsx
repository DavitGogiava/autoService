export const metadata = {
    title: "AutoService",
    description: "dynamic descriptions",
  };
  export default function layodut({ children }) {
    return (
      <>
        {children}
      </>
    );
  }